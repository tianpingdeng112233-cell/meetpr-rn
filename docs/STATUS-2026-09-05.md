# MeetPR 安卓 1:1 复刻 — 阶段报告(2026-09-05 07:10)

## 结论
学员侧(W0–W1)与教练侧(W2)全部实装完毕并在模拟器闭环走查,PR 栈 16 张等 David 审(全部 T2)。W3(播放器变速/标记/角标 + 图表 + 视觉 pass)已开侦察,i18n 收口卡在跑。

## PR 栈(按合并顺序;CI 见各 PR)
main ← #13 G0-a tokens ← #16 G0-b i18n ← {#17 G0-c Global 登录 · #14 W1-i onboarding/bind · #18 W1-g 成长 tab · #20 W1-d/W1-f 推进制训练环} ← #21 W1-p 我的(base w1i) · #22 W1-h 视频链(base w1d) ← #26 W2-a 壳+Dashboard+花名册 ← #25 W2-b 学员详情 ← #24 W2-d 我的+邀请码 ← #27 W2-c 消息+工作台+聊天
已合 main:#11 重 pin、#12 参照包、#15 W1 参照 v2 + CI 修、#19 GUID/movement_pattern DTO 修、#23 W2 参照包。

## 模拟器闭环(CN staging,教练 0047 / 学员 0048 / 新学员 0053)
- 学员:记组 → 长按结算 → Today 切下一节 → 撤销;成长 tab e1RM;我的 tab 全部页面;视频:选片 → 懒建日志 → initiate → 分片 PUT → complete,服务端 `GET /students/:id/videos` 出现附件。**录像本身模拟器不可验(QEMU 相机开录挂死、webcam 全黑+ANR),待真机。**
- 教练:Dashboard 待办/周概况 → 接收申请(资料页/确认 sheet/横幅) → 学员详情五段 → 收件箱 → 待反馈队列 → 工作台(播放/加标记/发反馈,服务端落库) → 聊天(Remind to train 预填草稿,消息落库) → 我的/邀请码/Help/Privacy。

## 走查期间修掉的真缺陷(都在对应 PR 里)
Fabric 嵌套 Modal 不显示(OverlayHost);image-picker 插件 false 删掉 CAMERA 权限;AppState 瞬时 background 自关录制页;`File.copy` 未 await 导致副本消失;`expo/fetch` File body 在 Android 原生层被拒 → 分片改 legacy uploadTask;OSS 预签名不能带 Content-Type;Metro 对 symlink node_modules 懒 import 解析错 → 静态 import;expo-camera 把 media3 抬到 1.9 → RNV 播放器崩 → patch-package;复数按 iOS countIndex 选参;体重 a11y 文案误显;训练页头对齐 iOS。

## 待 David 拍板 / 亲手
1. 教练端「排计划」入口:iOS 202e95db 已无 app 内排计划,也没有 plan-web 外链;RN 是否加「在网页排计划」外链?(A 不加照抄 iOS / B 加外链)
2. 角标烧录导出(spec 078)安卓 v1:A 做(需 FFmpeg 类能力,与 AGENTS 红线冲突)/ B 先不做,PARITY 标偏差(推荐 B)
3. 视频真后台续传(前台服务)另开小卡与否(推荐 v1 不做,冷启/回前台续传已有)
4. 亲手项:Google Cloud Android OAuth client、Play 开发者账号、release keystore、Global backend 部署(#277 已合 staging)、真机录像验收(iOS 端同款账号可对照)。
5. 发现的 iOS 正典问题(不改,只报):`student.trainingCalendarLogic.copy011` 「日」→ "Sun" 误译;两处 `zh_Hans_CN` 硬编码日期格式(教练视频墙段标题、资料页 joinedAt)。

## 已知栈依赖
- W2-b 学员资料段依赖 w1i 的 onboarding schema(main 上旧 schema 把 `training_days` 当数字 → 「资料不可用」),合并 #14 后自愈。
- W1-g 合 main 需补 `history/model.ts` 适配(PR #18 评论里有 diff);W1-p 叠 w1d 时休息计时 hunk 取「实际 RPE 回退处方」。
- CN staging 走查不代表 Global 生产(api.meetpr.app)可用;Global 登录(#17)需 David 的 Google OAuth client 后真机验。
